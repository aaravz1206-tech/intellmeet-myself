{
  "name": "intellmeet-root",
  "version": "1.0.0",
  "description": "Root project to run frontend and backend together",
  "scripts": {
    "install:all": "npm install && npm install --prefix intellmeet-backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix intellmeet-backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
